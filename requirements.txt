pdfplumber>=0.10.0
PyMuPDF>=1.23.0
easyocr>=1.7.0
openpyxl>=3.1.0
camelot-py[cv]>=0.11.0
pycountry>=23.12.11
# All pip-installable, no system binaries required (no tesseract-ocr, no poppler-utils).
# Note: easyocr pulls in torch, so this install is a larger download (~1-2 GB),
# and easyocr downloads a small recognition model the first time OCR actually runs
# (needs internet access at that moment, not just at pip-install time).
# pycountry is optional - it gives the full ~249-country ISO list for country
# detection. If it's not installed, the script still works using a smaller
# built-in ~150-country list.
